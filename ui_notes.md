# DAZZ UI 参考截图分析

## 相机选择页（pasted_file_j0il93）
- 全黑背景
- 顶部：Dazz Pro 横幅（蓝→红渐变，圆角）
- 相机分类：Video / Photo / Option
- 每行5个相机图标，图标下方有名称
- 右上角：SAMPLE 按钮 + 菜单按钮
- 底部：下箭头收起按钮（圆形）

## 相机预览页（pasted_file_JKBRIX）
- 全黑背景
- 顶部有绿色小圆点（相机指示灯）
- 预览区：占屏幕约60%高度，有圆角，4:3比例
- 预览右上角：··· 菜单按钮（白色）
- 预览底部叠加层（左→右）：
  - 左：禁用网格图标（灰色圆角矩形背景）
  - 中左：温度计图标 + 数值（白色圆角矩形背景）
  - 中：数字"24"（焦距/mm，深灰背景）
  - 中右：曝光图标 + "0"（深灰背景）
- 预览下方快捷操作栏（4个图标，横排居中）：
  - 图片导入（相框+加号）
  - 定时器（圆形时钟）
  - 闪光灯（闪电）
  - 翻转摄像头（旋转箭头）
- 底部工具栏：
  - 左：无（或图库缩略图）
  - 中：大圆形快门按钮（白色，有黑色圆环）
  - 右：相机型号图标（OFM，带β标记）
- 最底部：小横条（收起/展开）

## 图库页（pasted_file_ZaBHWL）
- 全黑背景
- 顶部：返回箭头 + "全部照片" 下拉 + "选择"
- 第一格：导入图片（橙色图标）
- 3列网格布局
- 底部左下角：相机图标（圆形黑色背景）

## 照片预览页（pasted_file_vGzWtu）
- 照片全屏展示（带边框效果，如拍立得）
- 底部：相机型号标签（# FQS）+ 下载 + 删除

## 关键颜色
- 背景：#000000
- 控件背景：rgba(255,255,255,0.15) 或 #1C1C1E
- 文字：白色
- 强调色：橙色 #FF6B00
- 快门按钮：白色填充 + 黑色细圆环

## 12920.jpg - 相机预览主界面（关键）
- 全黑背景，顶部绿色小圆点（相机指示灯）
- 预览区：从屏幕左边到右边，有小圆角，4:3比例（不是1:1）
- 预览右上角：··· 三点菜单（白色）
- 预览底部叠加（从左到右）：
  - 禁用网格图标（灰色圆角矩形背景，左对齐）
  - 温度计图标（白色圆形背景）
  - "24"（焦距mm，深灰圆角矩形）
  - 曝光图标 + "0"（深灰圆角矩形）
- 快捷操作栏（4个图标，横排）：
  - 图片导入（相框+加号）
  - 定时器（圆形时钟）
  - 闪光灯（闪电）
  - 翻转摄像头（旋转箭头）
- 底部工具栏：
  - 左：无（空白）
  - 中：大圆形快门（白色填充，黑色细圆环）
  - 右：相机图标（OFM，带β蓝色标记）
- 最底部：小横条（收起/展开）

## 12922.jpg - 时间水印面板
- 相机预览缩小到上方（保留预览）
- 底部弹出面板（白色背景）：
  - 标题："时间水印" + 右侧"无水印"按钮（圆角矩形）
  - 子标签：颜色 | 样式 | 位置 | 方向 | 大小
  - 预览区：黑色背景，橙色数字时间戳 "2 25 '22"
  - 颜色选择：彩虹/绿/黄/橙/橙红/红/粉/蓝/黑 圆形色块

## 12923.jpg - 边框面板
- 相机预览缩小到上方
- 底部弹出面板（白色背景）：
  - 标题："边框" + 右侧"无边框"按钮
  - 子标签：样式 | 背景
  - 边框样式：随机/黄色/白色/绿色/黑色 方形色块
  - 白色色块下有蓝色勾选标记（当前选中）

## 12915.jpg - 图库页面
- 全黑背景
- 顶部：返回箭头 + "全部照片" + 下拉箭头 + "选择"
- 第一格：导入图片（橙色图标，黑色背景）
- 3列网格，照片有边框效果（白色边框，类拍立得）
- 底部左下角：相机图标（圆形深色背景）

## 12916.jpg - 照片预览页
- 全黑背景，照片居中
- 照片有拍立得边框（白色边框+花纹背景）
- 照片右下角有橙色日期戳
- 底部：# FQS（相机型号标签）+ 下载图标 + 删除图标

---

## 相框（底片）默认规则 — 强制执行，新增相机必须遵守

### 规则一：默认相框只有拍立得相机才开启

| 相机 category | defaultSelection.frameId | 说明 |
|---|---|---|
| `instant`（inst_c / inst_s / inst_sq 等） | `"instant_default"` | 第一次打开默认带相框，且根据默认比例自动匹配 |
| 其他（ccd / digital / film / creative 等） | `null` | 第一次打开无相框，由用户自行选择 |

**新增相机时**：
- 若 `category == "instant"` → `defaultSelection.frameId` 必须设为 `"instant_default"`（或对应的默认相框 ID）
- 若 `category != "instant"` → `defaultSelection.frameId` 必须设为 `null`

### 规则二：相框选择受比例限制

- `FrameDefinition.supportedRatios` 为空 → 所有比例均可选
- `FrameDefinition.supportedRatios` 非空 → 只有在列出的比例下才显示该相框
- `RatioDefinition.supportsFrame == false` → 该比例下相框按钮隐藏，且切换到此比例时自动清除已选相框

### 规则三：保留设定开关（底片）

保留设定 → 底片（`retainFrame`）开关控制的是 `activeFrameId`：
- 开关**关闭**（默认）：切换相机时，相框恢复为该相机的 `defaultSelection.frameId`
- 开关**开启**：切换相机后再切回来，恢复上次为该相机选择的相框（从 `CameraSnapshot.frameId` 读取）

快照保存时机：每次切换相机前，`_saveCurrentSnapshot()` 将当前 `activeFrameId` 写入该相机的快照。

### 规则四：相框 UI 过滤

`_FrameGrid` 组件必须传入 `activeRatioId`，只展示 `supportedRatios` 包含当前比例（或为空）的相框。
